def reflexivity(dependency):
    #If  ϐ⊆ α, then α->ϐ 
    #This is pretty much asking for me to find all the 
    #Subsets of each right side
    beta =  []
    deps = #Extract dependencies somehow
    def decisionTree(i):